# Day 11

## 今日目标

明确第三周三因子 Factored MDP（因子化马尔可夫决策过程）的符号、状态结构和任务边界，完成编码前思考题，并为通用环境、三张因子图和 `independent` anchor（锚点任务）写出可执行的规格。

## 完成情况

- 完成 `docs/week3_questions.md`，回答任务书中的 11 道思考题，重点区分了完整 Markov state（马尔可夫状态）、层级上下文 $c=(k,b)$、显式因子分解和跨上下文转移共享。
- 说明 $x=(l,c)$ 与 $x=(l,k,b)$ 表示同一个联合状态，同时解释代码为什么仍需分别保存 Key 和 Beef 的结构。
- 区分 structural coupling（结构耦合）与 query-level coupling（查询级耦合），并说明为什么比较任务时需要单独控制最优原语路径长度 $L^*$。
- 创建 `docs/factored_mdp_spec.md`，记录三张 $3\times3$ 因子图、联合状态、动作集合、初始状态、终止条件、奖励、固定动作顺序和任务配置接口。
- 在规格中明确 `states` 必须从初始状态沿实际合法动作执行 BFS（广度优先搜索）得到，不能直接枚举 729 个理论组合。

## 当前地图

下图是 Location（位置）因子的字符展示。程序中坐标写作 `(row, col)`，原点 `(0,0)` 在左上角。

```text
             col=0           col=1           col=2

row=0       (0,0) --------- (0,1)    W      (0,2)
               |               |               |
row=1     B  (1,0) -------- C (1,1) --D--     (1,2)
               |               |               |
row=2     S  (2,0) --------- (2,1)    W    G  (2,2)
```

- `S` 是起点，`G` 是终点，`B` 是 cutting board（案板），`C` 是 kitchen（厨房）。
- `W` 表示墙，分别阻断 `(0,1) <-> (0,2)` 和 `(2,1) <-> (2,2)`。
- `D` 是 `(1,1) <-> (1,2)` 之间的双向候选门边。在 `key_gates_location` 和 `combined` 中，只有 Key 状态为 `(2,2)` 时才能通过；其他两个 anchor 中它是普通通路。
- 在 `location_gates_beef` 和 `combined` 中，`cook` 只能在 `C` 执行，`cut` 只能在 `B` 执行。

地图只展示联合状态的 Location 坐标；Key 和 Beef 从一开始就是状态因子，不是地图上的拾取物。完整初始状态和目标状态分别为

$$
x_0=((2,0),(0,0),(0,0)),
\qquad
x_G=((2,2),(2,2),(2,2)).
$$



## 遇到的问题

任务书中的“第一版令 $k=2$”与 Key 状态变量 $k$ 使用了同一个符号，当前无法仅凭上下文确定它指耦合数量还是其他参数。该问题已在规格中保留，没有自行补充含义。

$K_{L\to B}=2$ 的 transition template（转移模板）存在计数层级问题。当前 Beef 图可以按两类功能区规则、四个动作或实例化有向边计数；Day 11–12 的 `independent` 耦合为 0，不受影响，但 Day 13 实现耦合比例前必须统一定义。

> 后续处理：Day 15 将动作模式数记为 $S$，具体有向模板数保留为 $K$；当前 Beef 设计满足 $S_{L\to B}=2$、$K_{L\to B}=12$，详见 `progress/day15.md`。

思考题有些问题还需要深入探究琢磨，比如，如何设计未见过的 $(l,k,b)$ 组合？



## 下一步

Day 12 根据规格实现三张通用因子图、任务配置、`independent` 环境和 BFS 可达状态枚举，并添加 Markov state、因子不变性、动作合法性、终止条件和断连图测试。
